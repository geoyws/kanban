# /quality — 2026-08-31 13:06 MYT

**HEAD:** `583f02456919ab2f83477a20b368b689ecd3ab25` — `merge: land semantic watch envelopes` plus the uncommitted durable-subscription candidate
**Verdict:** FAIL

## Gates

| Gate | Result | Details |
| --- | --- | --- |
| Lint | PASS | `cargo clippy --all-targets --all-features --locked -- -D warnings`; 0 errors, 0 warnings. |
| Typecheck | PASS | `cargo check --all-targets --all-features --locked`; all Rust targets and features checked. |
| Format | PASS | `cargo fmt --all --check` and `git diff --check`; no formatting or whitespace defects. |
| Coverage | FAIL | Full repository: 11,344/13,791 lines, 82.26%, below the 90% `/quality` floor. Durable-subscription executable changed lines: 627/627, 100%. |
| E2E presence | PASS | 23/23 critical process-boundary requirements mapped in `docs/testing/compiled-rust-e2e-matrix.md`; all 99 compiled-process tests passed. |

## E2E coverage audit

The canonical matrix maps every current critical flow to `tests/e2e.rs`:

- persistence/restart, multiple worktrees, atomic ownership, handoff, stale-token exclusion, and secret-safe read models;
- both atmux import formats, operations, audit safety, and legacy-schema compatibility;
- pull routing, read-only scheduling, story lifecycle, and bounded projections;
- SQLite-native RAG, MCP parity, served search, cursor-native watch, and durable subscriptions;
- deployment ledger, deployment self-archive, and browser-backed operator approval.

The durable-subscription row exercises schema-v21 migration; add, list, show,
pause, and resume across compiled process boundaries; audit redaction; malformed
selectors; instrumented storage-fault unit tests; generated schema metadata;
immutable identity; and board isolation. The full instrumented run measured 95 unit tests and 99
compiled-process tests passing. No test was skipped.

## Lint, typecheck, format, and coverage details

The subscription candidate passes Rust check, clippy with warnings denied,
rustfmt, whitespace validation, and the official skill validator. LLVM coverage
was collected with `cargo-llvm-cov` across every target and feature, including
real loopback HTTP/WebSocket and Chrome flows outside the sandbox.

The changed Rust paths have 100% executable-line coverage: `rust/lib.rs` 89/89
and `rust/store.rs` 538/538. `rust/db.rs`, `rust/model.rs`, and `rust/watch.rs`
add or centralize declarative schema/model/event-kind rows with no
LLVM-measurable executable lines. The full repository remains below the generic
90% floor. The largest pre-existing deficit is the
long-running server surface; child processes terminated after HTTP/browser
assertions cannot flush every LLVM profile before exit.

## Recommended next actions

Do not label the repository-wide `/quality` gate as passing. A separate
coverage-hardening slice must raise full-repository line coverage from 82.26%
to at least 90% without weakening measurement. The durable-subscription slice
itself introduced no uncovered executable changed line.
