# /quality — 2026-09-01 00:57 MYT

**Feature HEAD:** `dda6c085f87dcf498f9a4025cf21be6b881683ab` — `feat: add indexed recap windows`
**Verdict:** FAIL

## Gates

| Gate | Result | Details |
| --- | --- | --- |
| Lint | PASS | `cargo clippy --all-targets --all-features --locked -- -D warnings`; 0 errors and 0 warnings. |
| Typecheck | PASS | `cargo check --all-targets --all-features --locked`; every Rust target and feature checked. |
| Format | PASS | `cargo fmt --all -- --check` and `git diff --check`; no formatting or whitespace defects. |
| Coverage | FAIL | Full repository: 16,321/18,984 lines, 85.97%, below the 90% `/quality` floor. No threshold or source-path exclusion changed. |
| E2E presence | PASS | 8/8 dispatcher compiled-process tests and 100/100 existing compiled-process tests passed in the final instrumented run. The latter includes real loopback HTTP/WebSocket and real Chrome coverage. |
| Release build | PASS | `RUSTFLAGS='-D warnings' cargo build --release --locked --bins`; all three binaries built. |

## Test and E2E evidence

The final instrumented all-target run measured 179/179 unit tests, 8/8
dispatcher compiled-process tests, and 100/100 existing compiled-process tests
passing. No test was skipped. The new compiled-process case proves schema-v23
migration, exact half-open millisecond bounds, archive visibility, newest-first
ordering, validation of negative and reversed bounds, and generated schema
metadata through the compiled binary and a real SQLite database.

An earlier sandboxed E2E attempt observed five loopback-denied cases and stale
schema/index assertions. The stale assertions were repaired; the complete
unsandboxed suite then passed 100/100. The sandbox restrictions are not counted
as product failures, and no assertion or threshold was weakened.

## Coverage details

Coverage improved from the preceding 16,061/18,727 lines (85.76%) to
16,321/18,984 lines (85.97%). Relevant tracked-file totals are:

- `rust/db.rs`: 662/696 lines, 95.11%.
- `rust/lib.rs`: 2,354/2,531 lines, 93.01%.
- `rust/store.rs`: 6,282/6,627 lines, 94.79%.

The final measurement used
`cargo llvm-cov --all-targets --all-features --locked --summary-only` with the
matching rustup Rust 1.96.1 and LLVM 22 binaries so the browser and
loopback-backed process tests were included.

## Required follow-up

Do not label the repository-wide `/quality` or `/tidy` gate as passing. The
existing coverage-hardening task must raise full-repository line coverage to at
least 90% without excluding reachable source or weakening the measurement.
