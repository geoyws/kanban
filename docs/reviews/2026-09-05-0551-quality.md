# /quality — 2026-09-05 05:51 MYT

**Code-under-test HEAD:** `a558868461307f70da821ac1d3f079c6ffd66379`
**Verdict:** PASS

## Gates

| Gate | Result | Details |
| --- | --- | --- |
| Lint | PASS | `cargo clippy --all-targets --all-features --locked -- -D warnings`; 0 errors and 0 warnings. |
| Typecheck | PASS | `cargo check --all-targets --all-features --locked`; every target and feature checked. |
| Format | PASS | `cargo fmt --all -- --check`; clean. |
| Wrapper / diff hygiene | PASS | `skills/kb` wrapper tests pass; `git diff --check` clean. |
| Coverage | PASS | Instrumented run: 28,136/30,659 lines = 91.77076877915131%; 45,436/51,459 regions = 88.29553625216191%; 2,119/2,440 functions = 86.84426229508196%. Same three ignores as the isolated run. No exclusion, threshold, or assertion weakened. |
| Compiled-process E2E presence | PASS | Overall compiled-process targets and the main tests cover the repository's six business-critical flow groups mapped by the matrix; release-specific tests exercise all six real Cargo binaries. The complete instrumented suite passed. |
| Fixed-FD isolated unit evidence | PASS | Two ignored fixed-FD remap unit tests ran serially in isolation: 2/2 PASS. The third ignored serve-child fixture is invoked by its non-ignored parent. |
| Release build | PASS | `RUSTFLAGS='-D warnings' cargo build --release --locked --bins`; all six binaries built with warnings denied. |

## Test and suite evidence

Full isolated `cargo test --all-targets --all-features --locked`: 549 passed across 12 suites, 3 ignored. Suite breakdown:

- 351 library tests passed (3 ignored in the library target);
- 2 Claude adapter tests passed;
- 15 Codex App Server adapter tests passed;
- 1 Codex queue adapter test passed;
- 10 dispatcher tests passed;
- 170 main tests passed.

The two ignored fixed-FD remap unit tests do not run under the normal lib target serialization and were separately run serially: 2/2 PASS. The third ignored test is the serve-child fixture, which is invoked by its non-ignored parent rather than run standalone.

Instrumented `cargo llvm-cov --all-targets --all-features --locked`: 550 passed with the same three ignores.

No production Rust changed during this repair, so there is no new executable-Rust changed-line percentage to report; the shell changes are covered through the compiled-process layer below and are not llvm-cov-instrumented.

## Compiled-process layer classification

The compiled-process tests are the E2E layer for this change. They assert release behavior end to end through compiled processes: correct version flags; HIG authorization refuses a mismatched canonical HAX activation before any SSH; HAX root hidden during remote install; identical HAX/HIG package and binary bytes with matching canonical receipt fields (target/install metadata legitimately differs, so receipts are not byte-identical); remote retention; rollback; and cleanup. These are the shell regression evidence: the shell behavior is proven through the compiled processes that drive it, not by shell unit tests.

The 549-pass isolated run and the 550-pass instrumented run above are the measured basis; "main E2E" is a cargo suite name within those totals, not a claim that the whole suite is e2e.

## Boundary and residual risk

Shell lines are not llvm-cov-instrumented; their behavior is proven through the compiled-process tests rather than by coverage counters. The report proves behavior through compiled processes on the build host; no live-tier deployment and no served-commit verification is claimed here. No coverage exclusion, threshold, assertion, or timeout was weakened. No UI changed.

## Status

`/quality` verdict PASS. Production deployment and served-commit verification are not claimed by this report.

_2026-09-05 05:51 MYT_
