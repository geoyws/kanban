# /quality — 2026-09-01 02:21 MYT

**Feature HEAD:** `9a56a3298dcb578eaeb1bfb3e814bdad873f7c84` — `fix: exclude tag-only generated id matches`
**Verdict:** FAIL

## Gates

| Gate | Result | Details |
| --- | --- | --- |
| Format | PASS | `cargo fmt --all -- --check` PASS; `git diff --check` PASS. |
| Typecheck | PASS | `cargo check --all-targets --all-features --locked` PASS. |
| Lint | PASS | `cargo clippy --all-targets --all-features --locked -- -D warnings` PASS. |
| Release build | PASS | `RUSTFLAGS='-D warnings' cargo build --release --locked --bins` PASS. |
| Coverage | FAIL | Repository coverage: 16,459/19,140 lines = 85.99%, below the 90% `/quality` floor. No threshold, exclusion, or assertion was weakened. |
| E2E presence | PASS | Instrumented `rustup run stable cargo llvm-cov --all-targets --all-features --locked --summary-only` PASS for 183/183 unit, 8/8 dispatcher compiled-process tests, and 101/101 main compiled-process E2E. Real loopback/browser tests were included. |

## Test And Contract Receipts

The final instrumented run also preserves the earlier contract receipts:

- 100/100 repeated concurrent-init focused stress already ran before the final all-target gate.
- Two reload E2Es passed.
- The deployment archive canonical-ID regression passed.
- The new tag-only exclusion E2E and natural-language preservation checks passed.

No test was skipped. No threshold, exclusion, or assertion was weakened.

## Coverage Details

Relevant tracked-file totals:

- `rust/search.rs`: 745/787 lines, 94.66%.
- `rust/lock.rs`: 80/92 lines, 86.96%.
- `rust/lib.rs`: 2,355/2,532 lines, 93.01%.

## Status

The repository-wide `/quality` and `/tidy` verdict remains FAIL solely because coverage is still below the 90% floor. `t-874fd5bc` owns raising it.
