# /quality — 2026-08-31 22:55 MYT

**HEAD:** `45e9c17a715ca8b05932606ab91e0691251e6cfe` — `merge: harden delivery invariant coverage`
**Verdict:** FAIL

## Gates

| Gate | Result | Details |
| --- | --- | --- |
| Lint | PASS | `cargo clippy --all-targets --all-features --locked -- -D warnings`; 0 errors and 0 warnings. |
| Typecheck | PASS | `cargo check --all-targets --all-features --locked`; every Rust target and feature checked. |
| Format | PASS | `cargo fmt --all -- --check` and `git diff --check @{u}..HEAD`; no formatting or whitespace defects. |
| Coverage | FAIL | Full repository: 16,061/18,727 lines, 85.76%, below the 90% `/quality` floor. No threshold or source-path exclusion was changed. |
| E2E presence | PASS | 8/8 dispatcher compiled-process tests and 99/99 existing compiled-process tests passed in the final instrumented run. The latter includes real loopback HTTP/WebSocket and real Chrome coverage. |
| Release build | PASS | `RUSTFLAGS='-D warnings' cargo build --release --locked --bins`; all three binaries built and the debug-only dispatcher crash seam was absent from the release binary. |

## Test and E2E evidence

The final instrumented all-target run measured 175/175 unit tests, 8/8
dispatcher compiled-process tests, and 99/99 existing compiled-process tests
passing. No test was skipped. The dispatcher suite covers the compiled binary's
CLI surface, missing configuration, success and selector resolution, durable
failure modes, pause/resume rechecks, concurrent exactly-one claiming,
post-success crash recovery, and SIGTERM handling.

An earlier non-instrumented integrated run observed one intermittent failure in
`dispatcher_failure_modes_are_safe_and_durable`: the first `exit` case reported
zero failures instead of one. The case then passed alone, the complete 8-test
dispatcher suite passed, and the complete instrumented all-target run passed.
No assertion or threshold was weakened. This remains a flake observation rather
than a reproduced product failure.

## Coverage details

Coverage improved from the pre-hardening 15,880/18,570 lines (85.51%) to
16,061/18,727 lines (85.76%). The final tracked-file totals include:

- `rust/adapter_process.rs`: 378/468 lines, 80.77%.
- `rust/adapter_protocol.rs`: 247/247 lines, 100%.
- `rust/db.rs`: 602/636 lines, 94.65%.
- `rust/dispatch.rs`: 878/919 lines, 95.54%.
- `rust/dispatcher.rs`: 725/762 lines, 95.14%.
- `rust/store.rs`: 6,100/6,445 lines, 94.65%.
- `rust/watch.rs`: 1,008/1,074 lines, 93.85%.

The accepted hardening batches add unit-level assertions for adapter-process
input/error handling and delivery-state identity validation. They do not add a
new E2E flow and are not described as E2E coverage.

## Required follow-up

Do not label the repository-wide `/quality` or `/tidy` gate as passing. A
separate coverage-hardening task must raise full-repository line coverage to at
least 90% without excluding reachable source or weakening the measurement.
